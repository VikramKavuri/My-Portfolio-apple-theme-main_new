fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-dotenv>=1.0.1
pydantic>=2.8.0

# keep it light — no torch / sentence-transformers / faiss
numpy>=1.26.0
scikit-learn>=1.5.0     # ok to keep for TF-IDF keywords; remove if you want it even lighter

openai>=1.42.0          # use OpenRouter with OpenAI SDK
httpx>=0.27.0
